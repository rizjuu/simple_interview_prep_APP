name: interview_prep
description: "InterviewPrep — AI interview practice and feedback"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  
  lottie: ^3.0.0
  cupertino_icons: ^1.0.8
  firebase_core: ^4.2.0
  firebase_vertexai: ^2.2.0
  firebase_ai: ^3.4.0
  speech_to_text: ^6.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/ai_intro.json
    - assets/aiai.json
